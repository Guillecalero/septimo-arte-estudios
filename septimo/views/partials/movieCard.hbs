    <div class="col-md-4">
        <div class="card player-card">
            <img src="{{image}}" alt="{{title}}" class="card-img-top">
            <div class="card-body">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">Descripción: {{description}}
                <button>
                    <a href="{{id}}">Añadir a favoritos</a>
                </button>
                </p>
            </div>
        </div>
    </div>
